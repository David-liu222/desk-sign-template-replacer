interface:
  display_name: "桌签模板替换器"
  short_description: "按原模板生成会议、就餐或拼音桌签并保留打印版式"
  default_prompt: "使用 $desk-sign-template-replacer 按餐桌签、会议桌签或拼音桌签原模板替换姓名并输出可直接裁剪的文件。"
policy:
  allow_implicit_invocation: true
